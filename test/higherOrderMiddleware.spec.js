import chai from 'chai';
import { higherOrderMiddleware } from 'higherOrderMiddleware';

describe('test', () => {
    it('test it', () => {
        // console.log(higherOrderMiddleware)
        // chai.assert.isFunction(1);
    });
});
